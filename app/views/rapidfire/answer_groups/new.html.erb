<h2>응답하기</h2>
<hr/>

<%= form_for([@question_group, @answer_group_builder]) do |f| %>
  <%- @answer_group_builder.answers.each do |answer| %>
    <%= f.fields_for("#{answer.question.id}", answer) do |answer_form| %><br>
      <%= render_answer_form_helper(answer, answer_form) %><br>
    <% end %>
  <% end %>
  <%= f.submit "Save" %>
<% end %>
